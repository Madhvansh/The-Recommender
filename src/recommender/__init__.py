"""The Recommender: a state-space (S4) sequential recommendation library.

Frames recommendation as next-item prediction over user interaction histories
and uses a from-scratch Structured State-Space (S4) backbone, benchmarked
against a SASRec self-attention baseline.
"""

__version__ = "0.4.0"
